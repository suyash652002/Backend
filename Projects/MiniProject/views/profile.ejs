<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 text-white p-8">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-2">Hello , <%= user.name %>
        </h3>
        <h5 class="mb-5">You can create a new post</h5>

        <form action="/post" method="post">
            <textarea placeholder="What's on your mind"
                class="block rounded-md mb-3 p-3 w-1/3 outline-none resize-none bg-transparent border-2 border-zinc-800 "
                name="content"></textarea>
            <input type="submit" class=" w-40 block px-3 py-2 bg-yellow-600 rounded-md" value="Create New Posts">
        </form>

        <div class="posts mt-20">
            <h3 class="text-xl text-zinc-400 ">Your Posts.</h3>
            <div class="postcontainer mt-5 ">
                <% user.posts.reverse().forEach((post) =>{ %>
                    <div class="post w-1/3 p-3 mb-5 border-2 border-zinc-700 rounded-md bg-zinc-800">
                        <h4 class="text-yellow-500"><%= user.username%></h4>
                        <p class="text-sm tracking-tight"><%=post.content%></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                        <div class="btns flex  gap-4">
                            <a class="text-yellow-500" href="/like/<%= post._id%>">
                                <%= post.likes.indexOf(user._id) ===-1 ? "like" : "unlike" %>
                            </a>
                            <a class="text-zinc-500" href="/edit/<%= post._id%>">Edit</a>
                        </div>
                    </div>
                <% }) %>
                

            </div>
        </div>
    </div>
</body>

</html>