<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500 text-l" href="/">Back to Home</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-4"> Users</h1>
        <div class="users flex gap-4 flex-wrap">
            <% if(users.length > 0) { %>
                <% users.forEach((user)=>{ %>
                    <div class="user max-w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-52 rounded-lg bg-zinc-700">
                            <img class="w-full h-full object-cover" src=<%=user.image%> alt="">
                        </div>
                        <h3 class="text-l mt-3 tracking-tighter"><%=user.name%></h3>
                        <h5 class="text-zinc-500  overflow-hidden"><%=user.email%></h5>
                        <div class="flex gap-5 mt-5">
                            <a class="text-yellow-500" href="/edit/<%=user._id%>">Edit User</a>
                            <a class="text-red-500" href="/delete/<%=user._id%>">Delete User</a>
                        </div>
                    </div>
                    <% })%>
            <% } else{ %>
                <h1 class="text-3xl tracking-tighter mb-4">No Users Yet</h1>
            <% } %>





        </div>

    </div>
</body>

</html>